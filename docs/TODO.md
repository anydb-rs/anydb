# TODO

- __CRATES__
  - _RAWDB_
  - _VECDB_
    - make serde an optional feature
    - vecs:
      - add lz4 option
      - add raw option (rename raw to zerocopy (and then make dep optional))
- __DOCS__
